# Downloaded models — never commit these
downloads/
*.part
*.part.json

# Local settings (contains HF / Civitai tokens)
settings.json
settings.local.json

# The download queue, and the copy kept of it before the library changed its schema
queue.db
queue.db-journal
*.before-library.bak

# Cached preview images — refetched on demand
previews/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/

# Editors / OS
.vscode/
.idea/
Thumbs.db
desktop.ini
